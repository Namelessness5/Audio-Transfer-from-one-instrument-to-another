Filter.py is used to check out whether your dataset is aligned.
For the detail of preprocess, please check out the utils.py and preprocess.py.
record.py is used to record.
unzip all the zips
bestmodel doesn't means best
This models have different depth, conv_dim, kernel_size, You need to select by yourself.
